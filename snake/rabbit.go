package snake

import (
	"snake/util"
)

const rabbitSymbol = "v"

// Rabbit the rabbit
type Rabbit struct {
	elements *[]Printable
}

// NewRabbit make a new rabbit
func NewRabbit() {
	els = make([]Printable)
	return &Rabbit{elements: elements}
}
